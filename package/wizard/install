[
  {
    "stepTitle": "基础配置 / Basic Settings",
    "items": [
      {
        "type": "text",
        "field": "MUSIC_PATH",
        "label": "音乐根目录路径 / Music Storage Path",
        "placeholder": "例如：/vol1/1000/Music 或 /vol2/1000/下载/音乐",
        "rules": [{"required": true, "message": "请输入 NAS 上的音乐存储绝对路径"}]
      },
      {
        "type": "text",
        "field": "service_port",
        "label": "Web 服务端口 / Web Service Port（默认 8091）",
        "initValue": "8091",
        "placeholder": "8091",
        "rules": [{"required": true, "message": "请输入有效的端口号（如 8091）"}]
      },
      {
        "type": "tips",
        "helpText": "💡 存储与权限说明 (Storage & Permission Notice)：\n1. 卷映射机制：此处填写的物理路径将被直接挂载为容器内部的 /music 目录。进入 Web 控制台后，您可直接通过目录选择器快速点选整理源目录与输出目录（如 /music/整理前 与 /music/整理后），无需反复输入长路径。\n2. 系统读写权限：请确保该音乐文件夹在飞牛「文件管理」中已对本应用开放读写权限，以便进行元数据嵌入与无损整理。\n3. 随时平滑修改：后续若调整存储位置，可在飞牛「应用中心」随时点击本应用的「配置」更新路径并平滑重启生效。"
      }
    ]
  }
]

